# Exemplo de .env
# SMTP settings (opcional) - se não preencher, os contatos serão gravados em contact.log
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu_usuario
SMTP_PASS=sua_senha
SMTP_FROM=seu@dominio.com
CONTACT_TO=seu@dominio.com

# Porta do servidor
PORT=3000
